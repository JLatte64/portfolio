import React from "react";
import "../components/styles/ContentGallery.css"

export function ContentGallery({ children }: {children : React.ReactNode}) {
    return (
        <div className="gallery-container">
        </div>
    );
}